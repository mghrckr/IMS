SELECT 
    TO_CHAR(BORROW_DATE, 'Month') AS BULAN, 
    COUNT(*) AS JUMLAH_TRANSAKSI
FROM 
    BORROW_TRANSACTION
GROUP BY 
    TO_CHAR(BORROW_DATE, 'Month')
ORDER BY 
    TO_DATE(TO_CHAR(BORROW_DATE, 'Month'), 'Month');
